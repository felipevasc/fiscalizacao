import RotasInternas from '../../../routes';
import { StyledConteudo } from '../styles';

const Conteudo = () => {
  return (
    <StyledConteudo>
      <RotasInternas />
    </StyledConteudo>
  );
};

export default Conteudo;
